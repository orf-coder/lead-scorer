Lead Scorer
===========

Hybrid rule-based + ML lead scoring system.

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```

Deploy on Streamlit Community Cloud: connect your GitHub account, select repository `orf-coder/lead-scorer`, and publish the `main` branch.
