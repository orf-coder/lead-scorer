Lead Scorer
===========

Hybrid rule-based + ML lead scoring system.

Run locally:

```bash
pip install -r requirements.txt
streamlit run app.py
```

Deploy on Streamlit Community Cloud: connect your GitHub account, select repository `username/repo name`, and publish the `main` branch.
